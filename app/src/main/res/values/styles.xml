<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar" />
    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/dialogsaclein</item>
        <item name="android:windowExitAnimation">@anim/dialogscaleout</item>
    </style>

</resources>
